export default function Menu() {
  return (
    <nav className="lab-menu">
      <p>
        Use the header menu to navigate… That’s the only standard way for navigations I believe.
      </p>
    </nav>
  );
}